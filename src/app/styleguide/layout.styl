/*- 🎨 Base layout
 *
 * Responsible for all commons layout
 *
-*/

@import 'container';
@import 'spacing';

your-box-layout() {
  &__container {
    container();
    container-responsive();
  }
}

your-box-layout-responsive() {}
